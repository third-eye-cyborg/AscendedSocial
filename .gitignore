node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
*storybook.log
storybook-static
logs/
server.log
*cookies.txt
.builder-bridge.json

# Postman environment files with secrets (keep templates)
postman-environment-*.local.json
*-secrets.postman_environment.json
# Keep workspace config (no secrets)
!.postman-workspace.json
